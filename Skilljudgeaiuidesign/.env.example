# Frontend environment configuration
# Single source of truth for backend: VITE_API_BASE_URL only.

# Production (Render): Set to your backend service URL (HTTPS). Never use localhost.
# Example: VITE_API_BASE_URL=https://skill-judge-ai-backend.onrender.com
# Development: VITE_API_BASE_URL=http://localhost:8000 or leave empty for same-origin
VITE_API_BASE_URL=
